# Next Commit
This file will be updated on each commit.

## File operations
We will be working on file operations.
(`code/file.ts`)
The framework relies heavily on reading/writing files and scanning directories.


Next commit
===========

## Implement I/O Utilities
Develop `load_file(path: string)` and `write_file(path: string, content: string)` using `Deno.readTextFileSync` and `Deno.writeTextFileSync`.

We will use the "sync" version of these functions.
To keep things simple. This is a build time tool.
No need for realtime operations.



